#!/bin/sh
set -e

PKG=config-testbox

if [ "$1" = remove ] ; then
    rm -f /etc/testbox/hostname
    rm -f /etc/testbox/ser2net.conf
fi

invoke-rc.d ser2net start
invoke-rc.d r4dd start

#DEBHELPER#

exit 0
